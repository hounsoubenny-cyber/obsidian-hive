[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = .
python_files = test_*.py
addopts = -p no:logfire
markers =
    slow: tests lents (clonage, simulations complètes)
